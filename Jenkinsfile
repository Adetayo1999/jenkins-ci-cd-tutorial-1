pipeline {
     agent any

     stages {
         
         stage("test") {
             steps {
                 echo "Testing the aplication..."
             }
         }

        stage("build") {
            steps {
                 echo "Building the application..."
            }
        }

        stage("deploy") {
             steps {
                 echo "Deploying the application..."
             }
        }

     }
}